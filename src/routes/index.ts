const BASE_AUTH_ROUTE = '/auth';
const AUTH_ROUTES = {
  SIGN_IN: `${BASE_AUTH_ROUTE}/signin`,
  SIGN_UP: `${BASE_AUTH_ROUTE}/signup`,
};

const HOME_ROUTE = '/';

export { HOME_ROUTE, AUTH_ROUTES };
